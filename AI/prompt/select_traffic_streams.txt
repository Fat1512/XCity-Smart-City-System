NHIỆM VỤ: Trích xuất ID camera từ danh sách dựa trên địa điểm trong câu hỏi.

DANH SÁCH CAMERA HIỆN CÓ:
{streams_list}

QUY TẮC KHỚP LỆNH:
1. Tìm kiếm tên đường/địa điểm trong câu hỏi (bỏ qua dấu, chữ hoa thường).
2. So sánh với trường "address" hoặc "name" của camera.
3. Độ chính xác phải cao (Ví dụ: "Lê Lợi" không được khớp với "Lê Lai").

LOGIC TRẢ VỀ:
- Nếu câu hỏi không nhắc đến địa điểm cụ thể (VD: "Kẹt xe không?", "Giao thông thế nào?") -> Trả về: ALL
- Nếu tìm thấy tên đường khớp -> Trả về: stream_id
- Nếu nhắc đến địa điểm KHÔNG có trong danh sách -> Trả về: NONE

INPUT: "{query}"

OUTPUT (Chỉ trả về mã ID, ALL hoặc NONE):